.venv/
__pycache__/
*.pyc
.playwright-mcp/
.env
.env.*
!.env.example
.gcp-sa.json
*service-account*.json

# spike run artifacts — regenerable, and contain Google Places-derived content
spikes/**/*.json
spikes/**/ids.txt
spikes/**/*.out

# Corporate TLS-interception roots, needed only to build behind such a proxy. Local, never shared.
tp_backend/certs/*.crt
